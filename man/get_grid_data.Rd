% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cheetahr_shiny_utils.R
\name{get_grid_data}
\alias{get_grid_data}
\title{Get the current data state of a cheetah grid}
\usage{
get_grid_data(
  id,
  session = shiny::getDefaultReactiveDomain(),
  handle_rownames = FALSE
)
}
\arguments{
\item{id}{Character. The output ID of the cheetah grid widget.}

\item{session}{The Shiny session object. Defaults to the current session.}

\item{handle_rownames}{Either a logical or a character column name. If
\code{TRUE}, the first column in the data is converted to row names using
\code{tibble::column_to_rownames()}. If a character string, that column is
used as row names. If \code{FALSE}, no column is converted to row names.
Defaults to \code{FALSE}.}
}
\value{
A data frame containing the current state of the grid data,
or NULL if the data state is not available.
}
\description{
Retrieves the current data state from a cheetah grid widget and converts it
to a data frame. This function should be called within a reactive context
(e.g., inside \code{observe}, \code{observeEvent}, \code{reactive}, or \verb{render*} functions).
}
\details{
The function constructs the input ID by appending \verb{_data_state} to the
provided grid ID, retrieves the data from the Shiny input, and converts
the nested list structure to a data frame.
}
\examples{
\dontrun{
# In a Shiny server function:
server <- function(input, output, session) {
  observeEvent(input$grid_data_state, {
    # Use first column as row names (default)
    current_data <- get_grid_data("grid")
    print(current_data)
  })

  # Use a specific column as row names:
  current_data <- get_grid_data("grid", handle_rownames = "rownames")

  # Do not convert any column to row names:
  current_data <- get_grid_data("grid", handle_rownames = FALSE)
}
}

}
